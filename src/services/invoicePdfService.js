"use strict";

async function buildInvoicePdfBuffer(invoiceId, user) {
  // ✅ plug your existing logic here:
  // - if you already generate invoice PDF with pdfkit/puppeteer
  // - return Buffer

  throw new Error("buildInvoicePdfBuffer not wired yet.");
}

module.exports = { buildInvoicePdfBuffer };